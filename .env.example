# Database Configuration
# When running with docker-compose, DB_HOST defaults to 'db' (the service name).
# For local development, set DB_HOST=localhost.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=axon-db
DB_USER=user
DB_PASSWORD=password

# Application Port
SERVER_PORT=8080
